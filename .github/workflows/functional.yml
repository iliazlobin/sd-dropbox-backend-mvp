name: Functional

"on":
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 13 * * *"

permissions:
  contents: read

concurrency:
  group: functional-${{ github.ref }}
  cancel-in-progress: true

jobs:
  functional:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: dropbox
          POSTGRES_PASSWORD: dropbox
          POSTGRES_DB: dropbox
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Run migrations
        env:
          DROPBOX_DATABASE_URL: postgresql+asyncpg://dropbox:dropbox@localhost:5432/dropbox
        run: alembic upgrade head
      - name: Run functional tests
        env:
          DROPBOX_DATABASE_URL: postgresql+asyncpg://dropbox:dropbox@localhost:5432/dropbox
        run: pytest tests/functional/ -v
